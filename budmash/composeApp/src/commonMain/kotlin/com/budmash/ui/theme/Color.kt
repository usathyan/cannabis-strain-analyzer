package com.budmash.ui.theme

import androidx.compose.ui.graphics.Color

// Primary - vibrant green
val Green80 = Color(0xFF66BB6A)  // Brighter green for dark theme
val Green40 = Color(0xFF2E7D32)  // Dark green for light theme

// Secondary - gold/amber accent (matches logo)
val Gold80 = Color(0xFFFFD54F)   // Bright gold for dark theme
val Gold40 = Color(0xFFFFA000)   // Darker gold for light theme

// Tertiary - teal
val Teal80 = Color(0xFF4DB6AC)
val Teal40 = Color(0xFF00897B)

// Background/Surface - higher contrast
val SurfaceDark = Color(0xFF0D0D0D)   // Near black
val SurfaceLight = Color(0xFFFFFFFF)  // Pure white

// High contrast text - maximum readability
val OnSurfaceDark = Color(0xFFFFFFFF)   // Pure white text on dark
val OnSurfaceLight = Color(0xFF000000)  // Pure black text on light
